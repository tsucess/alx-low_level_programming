#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: the first integer
 * @b: the second integer
 * Return: mul.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
