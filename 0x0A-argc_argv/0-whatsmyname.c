#include <stdio.h>
/**
 * main - Entry point
 * @argc: counter parameter
 * @arvv: array parameter
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf ("%s\n", argv[argc]);

	return  (0);
}
